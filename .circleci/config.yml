version: 2.1

jobs:
  checks:
    docker:
      - image: cimg/rust:1.59-node
    steps:
      - checkout
      - run:
          name: Check formatting
          command: "cargo fmt -- --config-path .circleci/fmt.ini --check"

  coverage:
    docker:
      - image: corebreaker/rust-devtools-circleci:rust1.59-v0.2.3
    steps:
      - checkout
      - run:
          name: Make coverage
          command: ".circleci/coverage.sh"
      - run:
          name: Upload Coverage Result To Coveralls
          command: "coveralls -D target -i target/coverage/coveralls.json circleci"
      - store_artifacts:
          path: target/coverage

  test:
    docker:
      - image: cimg/rust:1.59
    steps:
      - restore_cache:
          key: project-cache
      - checkout
      - run:
          name: Run tests
          command: "cargo test"
      - save_cache:
          key: project-cache
          paths:
            - "~/.cargo"

workflows:
  coverage:
    jobs:
      - coverage

  all-tests:
    jobs:
      - checks
      - test
